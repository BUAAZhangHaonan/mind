# MIND v1 Legacy Path

MIND v1 was frozen on the local branch `v1` and tag `v1-freeze-before-v2` at HEAD `81e3444`.

The legacy packages were removed from `master` during the v2 cleanup. Use the branch or tag above for v1 reproduction and audit work.

`master` is now the v2 Stage 0 line. New work should start from `configs/v2`, `docs/v2`, `scripts/v2`, and `src/mind/trajectory`.
