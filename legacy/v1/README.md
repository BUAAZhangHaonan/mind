# MIND v1 Legacy Path

MIND v1 was frozen on the local branch `v1` and tag `v1-freeze-before-v2` at HEAD `81e3444`.

The legacy packages remain in the repository for v1 reproduction and audit work. They are not the v2 main path. New v2 work should start from `configs/v2`, `docs/v2`, and `src/mind/trajectory`.
