# MIND

MIND is a detection-only research codebase for multimodal hallucination. It studies whether pre-generation hidden states drift away from visual-grounded local manifolds, then measures that drift across layers with wavelet features.

This repository is being built around a clean, reproducible pipeline for object hallucination detection on open multimodal models.
