# Stage A Entrypoint

Stage A has not started in this branch.

Use the completed Stage 0 cache contract as the only runtime input when Stage A begins. Do not add Stage A config, training code, or analysis code in the structural migration pass.

Previous prompt and document replacements used the old strings `MIND v2`, `V2`, `v2_`, `outputs/v2_stage0`, `configs/v2/stage0`, `scripts/v2`, and `tests/v2`. Treat those strings as historical labels only. New Stage A work should use the current paths and labels.
