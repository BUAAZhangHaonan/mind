# Stage A Entrypoint

Stage A has not started in this branch.

Use the completed Stage 0 cache contract as the only runtime input when Stage A begins. Do not add Stage A config, training code, or analysis code in the structural migration pass.

Previous prompt and document replacements used old versioned labels and path roots for the stage root, config tree, script directory, and test directory. Treat those phrases as historical labels only. New Stage A work should use the current paths and labels.
