# Stage A Entrypoint

Stage A starts from the completed Stage 0 cache contract.

Use `outputs/stage0` as the Stage 0 root. Stage A writes to `outputs/stageA`.

Stage A tests representation hypotheses only. It does not validate the final MIND detector, and Stage B has not started.
